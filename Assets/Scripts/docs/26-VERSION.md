# 26 — Готовность по осям и версия

Замер на 14 сентября 2026. Заменяет трёхчастный замер
`20-READINESS.md`: три части оказались слишком крупными и прятали
разницу внутри себя — «представление» держало в одной цифре четыре
готовые модели и полный ноль звука.

Здесь девять осей, у каждой явный список пунктов, и версия **выводится
из списка, а не назначается на глаз**.

---

## 1. Как читается версия

### Стадия — по условиям входа, не по ощущению

| Стадия | Условие входа |
|---|---|
| **pre-alpha** | звена нет: либо не написано, либо написано и до игры не доведено |
| **alpha** | доходит до игрока, но неполно по составу демо или не проверено |
| **beta** | полно по составу демо **и** проверено — машиной или глазами |
| **gamma** | отполировано: проход по `25-EYES.md` даёт ноль находок |
| **release** | выпущено |

### Числа — `сделано.проверено`

У каждой оси перечислены пункты. Первое число — сколько сделано,
второе — сколько **проверено**. Разрыв между ними и есть главная
болезнь проекта, вынесенная прямо в версию:

> `Вид — alpha 4.0` читается как «четыре вещи сделаны, ни одна
> не проверена».

«Проверено» значит: поймано стендом, самопроверкой, автопроходом
или увидено глазами в Unity. Не «написано и выглядит правильно».

### Версия проекта — по самой слабой оси, а не по средней

Средняя цифра утешает и врёт: игра выходит на своём худшем звене,
а не на среднем. Поэтому **стадия проекта равна стадии самой слабой
оси, от которой зависит демо.**

Формат: `0.<осей на бете и выше>.<осей на альфе>-<стадия>`.

---

## 2. Версия на 14 сентября

# `Sinbinder 0.4.3-alpha`

Четыре оси на бете, три на альфе, две в pre-alpha. Стадия — **alpha**,
потому что бой и вид, без которых демо не демо, на альфе.

---

## 3. Оси

### 3.1 Движок решений (AOS) — `beta 7.6`

Ядро продукта. То, ради чего игра существует.

| Пункт | Проверено чем |
|---|---|
| тринадцать модулей голосуют | стенд, перечислены в `BehaviourResolver` |
| потолок голоса `MaxVoice` | самопроверка |
| уверенность = разрыв / громкость | самопроверка |
| доля колебания | стенд, замер по девятерым |
| четыре ступени прозрачности | `TextRules` в `SelfCheck` |
| отказ рождает положение, а не грех | замер 7 сентября |
| сводка `AOSStats` | **не проверено: печатается, утверждения нет** |

116 самопроверок, `check_self` 24 из 24, стенд собирается и считает.

**Оговорка, снижающая оценку.** Три метода движка не вызываются
ниоткуда: `RumourManager.Spread`, `MemoryProcessor.RecordInteraction`,
`AOSEventHub.OnBetrayal`. Слухи никто не распускает, взаимодействия
никто не записывает, предательство никто не объявляет. Пока не
разобрано — беты с оговоркой, а не чистой беты.

**До gamma:** разобрать три сироты, дать `AOSStats` утверждение.

---

### 3.2 Бой — `alpha 6.3`

| Пункт | Проверено |
|---|---|
| приказы отдаются и оспариваются | да, стенд |
| своеволие видно словом над воином | да, замер 7.4 строки за бой |
| наезд камеры один раз на первое своеволие | да, замер ≤2 наезда |
| урон, смерть, падение тела | клип `Die` есть; **в Unity не открыт** |
| труп и душа появляются на поле | да |
| побег как отбор у края карты | **не проверено** |

**Что держит на альфе, а не на бете:**

* **добыча после боя не достаётся никому.** `CollectLootWithSquad`
  не вызван ни разу; через него единственный вызов раздачи ноши.
  Бой не окупается (`11-MISSING.md` §4);
* **`Attack` показывает покой** — воин бьёт стоя;
* `HarvestableSoul` — вторая система душ, которую никто не жнёт.

**До беты:** соединить добычу, закрыть `Attack` ретаргетом, снять
вторую систему душ.

---

### 3.3 Стратегия и склеп — `alpha 7.2`

Двадцать файлов в `Crypt/`: доска миссий, банки, полки, стенды,
связывание, улучшения, полигон, пересказ.

| Пункт | Проверено |
|---|---|
| вылазки считают добычу через `BodyWorth` | да, стенд |
| доска миссий платит монетой | **нет** |
| сума на шесть банок, предел работает | да, самопроверка |
| души ставятся на полки руками | **нет** |
| связывание: банка → тело → гнездо | **нет** |
| голем только с каменными оковами | да, одна калитка `CanBind` |
| полигон на четыре зоны | **нет** |

**До беты:** пройти склеп глазами — пять из семи пунктов не видел
никто.

---

### 3.4 Пролог (содержание демо) — `beta 8.7 (условно)`

Шесть долей после выреза сцен 6 и 7.

| Пункт | Проверено |
|---|---|
| доли ведутся действиями игрока, не секундами | автопроход, 18 шагов |
| страховки кричат при истечении | `Beat.Until` по устройству |
| совет и выбор старшего | автопроход |
| трофеи и тревога после сундука | автопроход |
| обучение жатве | автопроход |
| подкрепление и побег | автопроход |
| склеп, плата, конец демо | автопроход |
| реплики встречи, мораль в трёх фразах | **нет** |

**Почему «условно».** Автопроход печатает `ПРОЙДЕНО ЦЕЛИКОМ` даже
когда лог полон ошибок: `_failed` растёт только на истёкшем шаге
(`14-HANDOFF.md` §26). Пока приговор не считает того, что сам
записал, семь «проверено» — заявка, а не факт.

**До gamma:** починить приговор (§26.5), пройти глазами
по `25-EYES.md`.

---

### 3.5 Вид — `alpha 5.0`

| Пункт | Проверено |
|---|---|
| четыре оболочки, 21 кость, humanoid | **нет** |
| шестнадцать клипов (Idle, Walk, Flee, Die) | **нет** |
| четыре контроллера, шесть состояний | **нет** |
| 39 текстур, десять материалов ambientCG | **нет** |
| `restyle.py` — чужая модель в нашу палитру | **нет** |

**Ноль проверено — и это не придирка: ни одна модель не открыта
в Unity ни одной сессией.** Череп просвечивал именно поэтому.

**Чего нет вовсе:**

* `Attack` и `Talk` показывают покой;
* **девять воинов внешне не отличаются друг от друга** — главная
  невыполненная работа оси;
* **материалов Unity (`.mat`) ноль.** Текстуры скачаны, в материалы
  не собраны.

**До беты:** открыть в Unity и посмотреть; собрать материалы;
девять обликов.

---

### 3.6 Звук — `pre-alpha 0.0`

**Ноль файлов.** При этом в коде тринадцать мест обращаются
к `AudioClip` / `AudioSource` / `Resources/Sounds`.

То есть звук — не «не начат», а **труба без воды**: код его ждёт,
его нет, и доля 5 проходит молча (`16-PLAYTEST.md`).

**До альфы:** один файл — рог. Он единственный, чьё отсутствие
названо в документах поимённо.

---

### 3.7 Оболочка игры — `beta 7.0`

| Пункт | Проверено |
|---|---|
| сохранение и загрузка | **нет** |
| F5 / F6 — быстрые | **нет** |
| гнёзда сохранений | **нет** |
| режим обязательств: одно сохранение | **нет** |
| режим записан внутрь файла сохранения | **нет** |
| меню паузы на Escape | **нет** |
| стартовый экран | **нет** |

Полно по составу, но **ноль проверено**. По условиям входа это
не бета, а альфа; ставлю бету с оговоркой только потому, что состав
закрыт целиком и проверка здесь — один вечер, а не работа.

**До честной беты:** нажать F5, F6, Escape и посмотреть.

---

### 3.8 Проверяемость — `beta 5.4`

Сильнейшая ось проекта, и её стоит считать отдельно: в этом проекте
она первого класса, а не служебная.

| Пункт | Проверено |
|---|---|
| `check.py` — 240 файлов, 0 замечаний | сам собой |
| `check_self.py` — 24 ловушки, все срабатывают | сам собой |
| стенд балансировки собирается и считает | да |
| 116 самопроверок в игре | да |
| автопроход демо, 18 шагов, без человека | **приговор врёт (§26)** |

**Две дыры:**

* приговор автопрохода не считает ошибок и сработавших страховок;
* **37 публичных методов не вызываются ниоткуда** — правило на этот
  класс не заведено, хотя класс стоит проекту дороже всех.

**До gamma:** приговор, двадцать пятое правило на сироты.

---

### 3.9 Витрина — `pre-alpha 0.0`

Ни страницы, ни трейлера, ни снимков, ни `README` в корне.

Ось выделена отдельно не для порядка: она **единственная, где ноль
работы означает ноль шанса**, сколько бы ни было сделано в остальных
восьми.

**До альфы:** три снимка и одна строка описания.

---

## 4. Сводка

| Ось | Версия | Что держит |
|---|---|---|
| Движок решений | `beta 7.6` | три сироты в движке |
| Пролог | `beta 8.7` усл. | приговор автопрохода врёт |
| Оболочка игры | `beta 7.0` | не нажато ни разу |
| Проверяемость | `beta 5.4` | приговор, правило на сироты |
| Бой | `alpha 6.3` | добыча не достаётся, удара нет |
| Стратегия и склеп | `alpha 7.2` | пять пунктов не видел никто |
| Вид | `alpha 5.0` | ноль проверено, девять обликов |
| Звук | `pre-alpha 0.0` | труба без воды |
| Витрина | `pre-alpha 0.0` | ничего |

**`Sinbinder 0.4.3-alpha`**

---

## 5. Что поднимает версию быстрее всего

По цене за стадию, не по важности:

1. **Один вечер глазами** — `25-EYES.md` плюс склеп плюс F5/F6.
   Поднимает «проверено» сразу у четырёх осей: вид, оболочка,
   стратегия, пролог. Дешевле этого нет ничего.
2. **Приговор автопрохода** (§26.5) — снимает «условно» с пролога
   и дыру с проверяемости. Полчаса кода.
3. **Соединить добычу** — бой из альфы в бету, одно решение автора.
4. **Один звуковой файл** — рог. Звук из pre-alpha в alpha.
5. **Три снимка** — витрина из pre-alpha в alpha.

Ни один из пяти не требует писать много кода. Четыре из пяти —
работа автора, и это не случайность: **код перестал быть узким
местом проекта.**

---

## 6. Как пересчитывать

Замер повторяется теми же шагами:

1. пункты осей — из этого файла, они не меняются без причины;
2. «проверено» — только при ссылке на то, чем проверено;
3. стадия — по таблице условий входа в §1, не на глаз;
4. версия проекта — по **слабейшей** демо-критичной оси.

Если ось поднялась — поднять её строку здесь и пересчитать
`0.<бет>.<альф>`. Замер, оставшийся в переписке, не существует
(`13-DRIFT.md` §8).

---

## 7. Пересчёт на 17 сентября

Замер, оставшийся в переписке, не существует (`13-DRIFT.md` §8),
поэтому пересчитываю по тем же условиям входа из §1.

### Что сдвинулось

| Ось | Было | Стало | Чем |
|---|---|---|---|
| **Движок решений** | `beta 7.6` | **`beta 8.7`** | `OnBetrayal` соединён, `HasCoreMemory` перестал быть сиротой — его зовёт `TitleManager`. Сирот в движке 9 → 7, и четыре из семи — `RumourManager`, отложенный по замыслу |
| **Проверяемость** | `beta 5.4` | **`beta 7.6`** | правил `check.py` 24 → **27** (сироты, мёртвые ветки, список сборки), приговор автопрохода перестал врать, стенд перестал молча пропускать разделы и получил раздел ТИТУЛЫ |
| **Бой** | `alpha 6.3` | **`alpha 6.5`** | перебежчик действительно меняет сторону, умения стоят сил, отряд не замолкает после первого боя. Но добыча по-прежнему не достаётся никому, а `Attack` показывает покой — стадия та же |
| **Вид** | `alpha 5.0` | **`alpha 6.0`** | глаза под цвет греха у всех четырёх. **Проверено по-прежнему ноль** |
| Пролог, Оболочка, Стратегия, Звук, Витрина | — | без изменений | |

### Версия

# `Sinbinder 0.4.3-alpha`

**Цифры те же, и это не ошибка пересчёта.** Ни одна ось не сменила
стадию: на бете было четыре, осталось четыре; на альфе три, осталось
три. Двигались числа внутри осей, а не сами оси.

Причина одна на всех и названа в §5: **самая дешёвая работа — вечер
глазами — так и не сделана.** Пока «проверено» у вида ноль, а автопроход
ни разу не прогнан, версия стоять и будет, сколько бы кода ни прибавилось.

### Что это значит за три дня до показа

Стадия `alpha` держится боем и видом. Ни то, ни другое **не чинится
кодом**:

* бою нужен `Attack` — это ретаргет Mixamo, работа локальной сессии;
* виду нужно, чтобы его **открыли в Unity** — работа автора.

Мой слой — движок и приборы — на бете и до показа трогаться больше
не должен: `27-TRAILER.md` §6, заморозка с 18 сентября.
