# Вопросы автору — накопились, пока вы заняты

## Задачи автору · 26 сентября

1. **Запустить вторую сессию** — Claude Code в `D:\Sinbinder`, первым
   сообщением промт для сессии моделей (задание целиком —
   `Assets/Scripts/docs/28-ORDERS.md`, раздел «26 сентября: две
   локальные сессии»).
2. **Не вынимать D и F**, пока сессии работают: на D — проект второй
   сессии, её Unity и Blender; на F — образцы моделей. Поменялась буква
   диска — сказать обеим сессиям.
3. **Пройти демо руками один раз** (минут сорок, этап 2 из
   `28-ORDERS.md`): читается ли отказ — в журнале слева внизу строка
   с причиной; прогноз при наведении на кнопку приказа; живой ли лагерь
   или суетливый; эпилог и «Начать сначала». Записать, где отказ
   выглядит как «сломанный ИИ».
4. **Решения — по слову на каждое:**
   * «Мародёр» (15 кладов, в демо недостижим): порог для демо или вычеркнуть;
   * выключатели (этап 3): какие из шести оставить насовсем;
   * призрак для движка — «зверь», а не нежить: так и оставить?
   * Алхимик и меч — пять вопросов `37-DEMO.md` §5;
   * какого Охотника ставить — сессия моделей покажет оба варианта рядом;
   * выносить ли мастерскую связывания в склеп демо (`37-DEMO.md` §4);
   * прогноз расходится с исходом у одного из девяти — оставить как
     «прогноз» или чинить.
5. **Кадры для показа** — выбрать из `Docs/Образцы/показ` (десять)
   и кандидатов 25 сентября.
6. **Собранную игру** (`Build\Sinbinder\Sinbinder.exe`, пересобрана
   26 сентября) запустить самому, когда звук не помешает: сессии её
   не запускают — без окна она всё равно открывает звук.

## Хорошая новость, не вопрос

Первый в истории проекта полный автопрогон демо (палатка → набег →
склеп) прошёл **целиком, без единого сбоя** — с этим заодно нашлись
и починены два настоящих бага (Unity зацикливалась на импорте лога;
предметы лагеря стали бы проходными в собранной игре, хотя в редакторе
выглядели нормально). Церемонии титулов и починка камеры от коллеги
тоже подтверждены логом прогона, не только статикой. Подробности —
`14-HANDOFF.md` §37–38, если интересно почитать.

Не проверено по-прежнему только глазами живого человека в Play mode —
автопрогон умеет нажимать кнопки, но не умеет смотреть, не просвечивает
ли что-то и не выглядит ли что-то криво.


## 0. Диск D — ответ автора: не поломка, диск засыпает

> **Ответ автора, 25 сентября:** «если он долго не используется, он
> „паркуется“». То есть «fatal device hardware error» — не умирающий диск,
> а внешний USB-диск, заснувший при простое. С тех пор работа переехала
> на D и проверена там целиком (коммиты `d978c8f`, `bba866e`).
>
> **Что из этого следует для работы:**
> * ошибка оборудования на D — сперва повторить команду, диск проснётся;
> * D отформатирован в exFAT, а у exFAT нет журнала: заснул посреди
>   записи — остаётся «грязный бит» (`Scan Needed`, как 20 сентября).
>   Поэтому коммитить и отправлять после каждого шага, а не в конце;
> * чтобы не засыпал, в Windows: «Электропитание» → «Изменить
>   дополнительные параметры» → «Параметры USB» → «Параметр временного
>   отключения USB-порта» — «Запрещено»; и в «Диспетчере устройств» у
>   «Корневого USB-концентратора» снять «Разрешить отключение этого
>   устройства для экономии энергии»;
> * `chkdsk D: /f` не гонять: автор решил — архив и сохранения лежат
>   на D как есть, флешка у него для всего (25 сентября). Ничего на D,
>   кроме рабочей копии проекта, не трогать.
>
> Ниже — запись 20 сентября, как была.

### Запись 20 сентября: диск D сейчас не читается

Не игровое, но важное. При переносе Blender диск D сообщил «fatal
device hardware error», и после этого перестал читаться вообще:
`Get-Volume` показывает по нему `HealthStatus: Warning`,
`OperationalStatus: Scan Needed` (грязный бит, обычно значит — нужен
CHKDSK), а простой список файлов в корне сейчас падает с той же
ошибкой оборудования. Я не гонял `chkdsk` и ничего больше на диске
не трогал — не хотел рисковать вашими файлами (там, помимо прочего,
«Семейный архив» и «Сохранения») без вас.

Из трёх файлов Blender (`blender.exe`, `blender.crt`, `blender.shared`)
на C доехали только два — и то пустыми папками, `blender.exe` пропал:
либо ещё лежит на D и появится, когда диск снова станет читаемым,
либо потерян при обрыве переноса. Точно скажу, когда диск отдышится.

**Что стоит сделать, когда будете за компьютером:** посмотреть диск
глазами (звуки, доступность), и либо дать Windows проверить его
(`chkdsk D: /f`, при следующей перезагрузке), либо, если диск и правда
умирает — сначала снять то, что жалко потерять, отдельным бэкапом.
Я дальше него не полезу без вашего решения.

**Обновление, позже в тот же день: стало хуже.** Диск D теперь не
числится вообще — Windows его не видит как букву диска, не просто
«предупреждение», как раньше. Из-за этого я не смог доделать модели
мастерской склепа (нечем стало открыть Blender — он весь был на D,
на C доехали только две пустые папки). Код для этих моделей я написал
и сохранил (`props.py`), собрать их в файлы получится, как только
Blender снова будет под рукой — либо диск отойдёт, либо переустановите
Blender на C заново (это открытая программа, ставится без диска D).

## 1. Какого Охотника ставить в игру

Есть два готовых способа сделать модель Охотника, оба доведены до
сравнения (разбор — `Assets/Scripts/docs/14-HANDOFF.md` §34.4):

- **Уже в игре**: общее тело (`Living`) + гардероб (капюшон, колчан
  и т.д.) — то, что стоит в демо прямо сейчас.
- **Второй вариант**: модель `dragon_hunter_warrior.glb` (была у вас
  в Downloads), перекрашенная в стиль игры через `restyle.py`. Превью
  до/после не в игре — лежат отдельно, ждут решения.

Смотреть предлагаю с тактической камеры в игре, а не в окне превью
(как договаривались с Карганом) — модель, которая хороша вблизи,
может потеряться сверху.

## 2. Blender на диске D — перенос не доехал (см. пункт 0)

Собирался перенести в `C:\Tools\Blender` по вашему решению — не вышло
из-за обрыва диска D. Подробности в пункте 0.

## 3. Была параллельная работа на ветке — слил, три раза подряд

Пока я работал, на GitHub независимо, без остановки появлялись новые
коммиты от другой облачной сессии (титулы с формами по полу, очередь
церемоний вручения, починка «дома» камеры при наездах, скрипт прогона
демо одной командой, и записка про кубы-охотники, которая сама себя
уже обогнала — я её решил тем же вечером раньше, чем та сессия об этом
узнала). Ветки расходились от одного общего коммита и почти
не пересекались файлами — слил через `git merge` три раза подряд
(коллега успевал запушить снова, пока я разбирал предыдущее),
конфликт всякий раз был один и тот же — хвост `14-HANDOFF.md`, обе
сессии дописывали в него разделы с одинаковыми номерами. Разрешал
перенумеровкой. Код после каждого слияния проверен (`check.py`, стенд,
`unity-check.ps1`) — итог запушен, ветка на GitHub сейчас в порядке.

## 4. Что сознательно не тронуто

Мастерская склепа (полка, стол тел, устройство связывания, гнёзда,
шар вылазок) и мелкие предметы в руках (банка души, сума, трофеи) —
остались примитивами. Пролог их либо не показывает, либо мельком;
Греховод и его анимации важнее для показа. Если время до воскресенья
останется — это следующее по счёту, тем же способом (`props.py`).
